/*package katiostudio.rivence.Controlador;

import java.util.List;
import java.util.ArrayList;
import java.util.LinkedList;

/**
 * Created by cquinz on 29/5/16.
 */
/*
public class ServicioFilterCards {

    private final List<Servicio> originalList;
    private final List<Servicio> filteredList;

    public ServicioFilterCards(List<Servicio> originalList){
        this.originalList = new LinkedList<>(originalList);
        this.filteredList = new ArrayList<>();
    }

    /*  FORMATEO DE LISTA DE CADA TIPO   */
/*    public List<Servicio> performFilteringPaid () {
        filteredList.clear();
        for ( Servicio servicio : originalList){
          //  if (servicio.categoryId == true) filteredList.add(servicio);
        }
        return filteredList;
    }

    public List<Pago> performFilteringPending () {
        filteredList.clear();
        for ( Pago pago : originalList){
            if (pago.pagado == false) filteredList.add(pago);
        }
        return filteredList;
    }

    public List<Pago> performFilteringAll () {
        filteredList.clear();
        filteredList.addAll(originalList);
        return filteredList;
    }
}
*/